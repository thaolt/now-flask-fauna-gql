{
  "name": "supershop",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@gitlab.com:thaolt/supershop.git",
  "author": "Thao le <thaolt@songphi.com>",
  "license": "MIT",
  "scripts": {
    "postinstall": "cd www && yarn && cd ..",
    "build": "cd www && yarn build && rm -rf ../public && cp -rf dist ../public && cd .."
  },
  "dependencies": {
    "yarn": "^1.22.0"
  }
}
